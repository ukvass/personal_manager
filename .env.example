## Local SQLite (default)
DATABASE_URL=sqlite:///./tasks.db

## Postgres example (docker-compose)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/personal_manager
JWT_SECRET=change-me-in-prod
JWT_ALGORITHM=HS256
JWT_EXPIRE_MIN=60
